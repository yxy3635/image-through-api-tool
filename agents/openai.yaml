interface:
  display_name: "Image API Tool"
  short_description: "Call configurable image generation and edit APIs."
  default_prompt: "Use $image-api-tool to generate or edit an image through the configured image API."
policy:
  allow_implicit_invocation: true
